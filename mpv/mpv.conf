# ==========================================
# Configuración óptima de mpv para Anime & Streaming
# ==========================================

# Aceleración por hardware con NVIDIA RTX (Vulkan + libplacebo)
vo=gpu-next
gpu-api=vulkan
hwdec=auto-safe
keep-open=yes

# Calidad de escalado de alta fidelidad
scale=ewa_lanczos
cscale=ewa_lanczos
dscale=mitchell
correct-downscaling=yes
linear-downscaling=yes

# Idiomas preferidos: Español Latino prioritario, Español España, luego Japonés e Inglés
alang=es-419,es-la,es,spa,ja,jpn,en
slang=es-419,es-la,es,spa,en
audio-pitch-correction=yes

# Subtítulos
sub-auto=fuzzy
sub-font='sans-serif'
sub-font-size=48
sub-border-size=2.5

# Búfer extendido para streaming fluido y salto inmediato sin lag
cache=yes
cache-secs=300
demuxer-max-bytes=350M
demuxer-max-back-bytes=150M
demuxer-readahead-secs=120
force-seekable=yes
hr-seek=yes
hr-seek-framedrop=yes
audio-buffer=0.25

# Compatibilidad con streams de audio web
user-agent="Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:128.0) Gecko/20100101 Firefox/128.0"
referrer="https://flaswish.com/"

# Servidor IPC para sincronización acústica en tiempo real
input-ipc-server=/tmp/mpv-socket
